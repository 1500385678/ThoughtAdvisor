# ThoughtWeb · 2026-09-01 增量计划

> T4 未在 0831 24:00 后写 .plan,0901 由 T5(03:50)按 0901 巡检 P0 建议兜底 —— 恢复 `.plan/20260829.md`(0829 增量计划上下文),作为 1 个小变更入库。

## 今日项

**巡检 P0 兜底 · `.plan/20260829.md` 恢复**:0829 commit `3eaf287` 写入的 `.plan/20260829.md`(0829 增量计划 + 任务 2 启动承接说明)在 0831-0901 之间被工作树删除,`git status` 报 `D` 状态(D+2 静默),0901 巡检 P0 建议"0901 24:00 前二选一:恢复 / `git rm` 显式确认",推荐 (a) 恢复以保留 0829 上下文(3eaf287 的"计划 + 种子表"二元结构完整)。

## 今天做什么

- `git checkout HEAD -- .plan/20260829.md` 恢复工作树文件(39 行,内容为 0829 T4/T5 兜底写的增量计划)。
- 新增 `.plan/20260901.md` 作为今日 T5 自兜底记录(沿用 0826/0829 惯例),让"恢复动作"显式落到 commit,避免 0902 巡检判定为"沉默恢复"。
- **不**改 `项目开发计划.md`(本次 P0 是结构性文件恢复,不是 Phase 0 任务推进,无对应 checkbox 可勾;任务 2-6 仍按 0901 巡检状态保留)。

## 涉及文件

- 恢复:`.plan/20260829.md`(回到 HEAD 状态,git 无净变更)
- 新增:`.plan/20260901.md`(本文件)
- 不动:`项目开发计划.md`、`data/` 下 4 个知识库文件(0829 入机,13/100 思想家稳定)

## 验收标准

- [x] `.plan/20260829.md` 文件回到工作树,`git status` 无 `D` 标记
- [x] `.plan/20260901.md` 写入,描述本次 P0 恢复 + T5 兜底上下文
- [x] 1 个 commit 显式说明 P0 决策(选 (a) 恢复,理由:保留 0829 增量计划上下文)

## 后续建议(给 T4 在 0902 写 .plan 时参考)

1. **0902 必启 Phase 0 任务 3(经典库)**:0901 巡检 P2 建议"0901 内启动 10 本里程碑经典 → `data/classics_v1.json`",0901 已 D+1 静默未启,0902 必须启动;字段 `{id, title_cn, title_en, author_id, era, school, topic_id, brief, representative_chapters}`,先 10 本凑起步。
2. **0902 必冻图谱 schema v1(任务 4)**:0831 巡检 P3 建议"0901 内冻结图谱 schema 最小版本(5 节点 + 6 关系)",0901 仍未启,0902 必须冻结;`data/graph_schema_v1.md` 落 `{thinker, school, classic, idea, era}` + `{belongs_to, wrote, influenced, criticized, contemporary_with, branch_of}`。
3. **0828+0829 累计 6 条 P1 建议仍待 T1 ack**:T1 飞书 p2p 统一 ack 列表(0828 P1.0/P1.1/P1.2/P1.3 + 0829 P1.1/P1.2),0902 24:00 前仍未 ack 将触发"系统性失能"判定。
4. **0830 漏检归因**:T1 查 `mavis cron list` 看 `inspect-思想-20260830` 是否 0830 02:50 触发但失败 / 还是根本没创建;0902 报告需明确归因(创建漏 / 调度漏 / 其他),避免 0903 又跳号。
