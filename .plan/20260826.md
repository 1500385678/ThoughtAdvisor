# ThoughtWeb · 2026-08-26 增量计划

> Phase 0 启动日(执行日)。T4 未在 0825 补 .plan,今晨由 T5(03:50)按 0824 遗留的 Phase 0 任务 1 实际落地。

## 今日项

**Phase 0 · 任务 1**:盘点 `_ThoughtLib/01-10` 10 大主题 md 并建索引。

## 今天做什么

- 读取 `_ThoughtLib/01-10/` 下 10 个主题 md,抽取 `id / 标题 / 朝代 / 核心人物 / 关键词 / 摘要 / 类别 / 字数`。
- 落到 `data/topics_index.json`(机读)+ `data/topics_index.md`(人读索引),作为后续 `thinkers.json / classics.json / graph schema` 的入口表。
- 0824 plan 原话:"本任务不修改大计划文件,留到 Phase 0 整体收尾时统一勾选"——本次**不**改 `项目开发计划.md` 的 checkbox。

## 涉及文件

- 新增:`data/topics_index.json`、`data/topics_index.md`
- 不动:`项目开发计划.md`(沿用 0824 plan 的"Phase 0 整体收尾时统一勾选"原则)

## 验收标准

- [x] `topics_index.json` 恰好 10 条,字段齐全无 null
- [x] `topics_index.md` 表格可读,每行 1 主题
- [x] 合计字数与源文件 `wc -m` 实测一致

## 后续建议(给 T4 在 0827 写 .plan 时参考)

1. **Phase 0 任务 2**:抽 100 思想家 → `thinkers.json` 入口表(可按 topics 倒推:每个主题下挂 10 思想家)
2. **Phase 0 任务 3**:抽 50 经典 → `classics.json` 入口表
3. **Phase 0 任务 4**:图谱 schema 设计(thinker / school / classic / idea / era 5 节点)
4. 0824 的 `思想顾问开发架构与计划.md` 仍 untracked,需 T1 在 README 仓库内务时纳入 git 并说明"两份计划的关系"
